pipeline {
  agent any
  stages {
    stage('compile'){
      when { not { branch '*' }tag ''}
      steps{
        echo "compile"
      }
    }
    stage('Test Cases'){
      when { expression { env.BRANCH_NAME != 'main'} }
      steps{
        echo "Test Case"
      }
    }
    stage('Docker Build'){
      when { expression { env.BRANCH_NAME != 'main'} }
      steps{
        echo "Docker Build"
      }
    }
    stage('Docker Push'){
      when { expression { env.BRANCH_NAME != 'main'} }
      steps{
        echo "Docker Push"
      }
    }
    stage('Deply to Dev'){
      when { expression { env.BRANCH_NAME != 'main'} }
      steps{
        echo "Deply to Dev"
      }
    }
  }
}
